{
  "name": "server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "dotenv -e ../.env tsx watch index.ts",
    "build": "tsc && tsc-alias",
    "start": "node dist/server/index.js",
    "lint": "eslint ."
  },
  "devDependencies": {
    "tsc-alias": "^1.8.16"
  }
}
